import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'school-book-storage',
  webDir: '../../dist/apps/school-book-storage',
  bundledWebRuntime: false,
};

export default config;
